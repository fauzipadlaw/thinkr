# Supabase Functions

- `evaluate_decision` — Calculates decision results (Weighted Sum Model). AHP / Fuzzy hooks are stubbed for future implementations.

## Deploy

```bash
supabase functions deploy evaluate_decision --project-ref <project-ref>
```

## Local serve

```bash
supabase functions serve evaluate_decision --env-file ../.env.local
```
